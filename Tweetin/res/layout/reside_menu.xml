<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:layout_width="match_parent"
             android:layout_height="match_parent">

    <ImageView android:id="@+id/iv_background"
               android:contentDescription="@null"
               android:adjustViewBounds="true"
               android:scaleType="centerCrop"
               android:layout_width="match_parent"
               android:layout_height="match_parent">
    </ImageView>

    <ImageView android:id="@+id/iv_shadow"
               android:contentDescription="@null"
               android:background="@drawable/rm_shadow"
               android:layout_width="match_parent"
               android:layout_height="match_parent"
               android:scaleType="fitXY">
    </ImageView>

    <ScrollView android:id="@+id/sv_left_menu"
                android:scrollbars="none"
                android:paddingLeft="35dp"
                android:layout_width="180dp"
                android:layout_height="match_parent">

        <LinearLayout android:id="@+id/layout_left_menu"
                      android:orientation="vertical"
                      android:layout_gravity="center_vertical"
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:layout_marginTop="5dp">
        </LinearLayout>

    </ScrollView>

    <ScrollView android:id="@+id/sv_right_menu"
                android:scrollbars="none"
                android:paddingRight="35dp"
                android:layout_width="180dp"
                android:layout_height="match_parent"
                android:layout_gravity="right">

        <LinearLayout android:id="@+id/layout_right_menu"
                      android:orientation="vertical"
                      android:layout_gravity="center_vertical"
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:layout_marginTop="5dp"
                      android:gravity="right">
        </LinearLayout>

    </ScrollView>

</FrameLayout>